# bird's fixed x coordinate
BIRD_X = 80
# bird height
BIRD_HEIGHT = 48

# the width of a pipe in pixels.
PIPE_WIDTH = 40
# vertical gap size between the two pipe ends
PIPE_GAP = 120
# Min height from the floor for the pipe opening
MIN_PIPE_HEGHT = 100
# pixels per frame that the pipes move at
PIPE_SPEED = 4
# space between 2 pipe sets
PIPE_SPACING = PIPE_SPEED * 50

GRAVITY = 0.5
# velocity given by a flap
FLAP_VEL = -8

BIRD_MAX_SPEED = 8

MAX_CEILING = 0
